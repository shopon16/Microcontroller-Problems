#define RESET_VALUE                 0           
#define VALUE_PRE_SCALE             4000            //Slow down the clock
#define TIMER_NUM                   0               // Order of Timer. We only have 4 timers so the order can be 0,1,2,3.
#define COUNT_UP                    true            //if it is true the timer will count up and vice versa.

#define AUTO_RELOAD                 true            //if it is true, timer will repeat.
#define VALUE_AUTO_RELOAD           1200000         //Timer will count upto this value and reset

#define SEC_60                      60
